{
  "name": "nbadraftpick-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Server-side Cloud Functions for the NBA 2K Draft Pick admin panel. Currently: an authenticated 'Backup Firestore' action + a read-only backup-history/status query, both called from the Admin panel via the Firebase Functions client SDK. The Firebase Admin SDK and all privileged Firestore/Storage access live ONLY here — never in the frontend.",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "true"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1"
  }
}
